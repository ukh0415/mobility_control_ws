# 프로젝트 작업 지침

이 파일은 저장소 루트와 모든 하위 폴더의 작업에 적용한다. 사람과 AI 작업자는 작업을 시작하기 전에 이 문서를 읽고, 코드보다 공용 프로토콜과 안전 규칙을 우선한다.

## 1. 기준 경로

- 저장소 루트: 현재 `AGENTS.md`가 있는 폴더
- PC 제어 프로그램: `pc/`
- ESP32 Arduino 프로젝트: `esp32/esp32_i2c_master/`
- STM32CubeIDE 프로젝트: `stm32/mobility/`
- 공용 통신 규격: `protocol/`
- 기술 문서와 문제 해결 기록: `agents/docs/`
- 코드 변경 이력: `agents/docs/코드_변경_기록/`
- 테스트: `tests/`
- 참고용 백업: `archive/`

STM32의 유효한 프로젝트는 이 저장소의 `stm32/mobility/`이다. `C:\STM32_Work\mobility` 등 저장소 외부의 과거 사본을 기준으로 수정하거나 빌드하지 않는다.

## 2. 작업 시작 전 확인

1. `AGENTS.md`와 작업 관련 문서를 읽는다.
2. 변경 대상이 Python, ESP32, STM32, 공용 프로토콜 중 어디인지 구분한다.
3. `protocol/protocol.md`의 명령 ID, 상태 ID, 단위 및 바이트 순서를 확인한다.
4. 기존 변경을 덮어쓰지 않도록 작업 트리 상태를 확인한다.
5. 하드웨어 구동이 포함되면 정지 조건과 인터록을 먼저 확인한다.

## 3. 구성요소별 책임

- Python은 사용자 입력, 로봇 전체 목표, 상위 상태와 로그를 담당한다.
- ESP32는 명령 라우팅, STM32 상태 취합과 통신 failsafe를 담당한다.
- STM32는 센서 판독, 감속비 환산, 위치·속도 제어, 로컬 상태 머신과 비상 정지를 담당한다.
- 안전에 직접 관련된 완료 판정과 인터록은 PC 통신에 의존하지 않고 STM32에서 수행한다.

## 4. 공용 프로토콜 변경 규칙

- 패킷 구조, 명령 ID, 상태 ID 또는 물리 단위를 임의로 한 구성요소에서만 변경하지 않는다.
- 프로토콜 변경을 먼저 `protocol/protocol.md`에 반영한다.
- Python, ESP32, STM32의 영향 범위를 문서에 함께 기록한다.
- 호환되지 않는 변경은 프로토콜 버전을 증가시킨다.
- 각도는 0.1도, 회전속도는 0.1 RPM, 거리 단위는 mm, 시간 단위는 ms를 기본으로 한다.
- 다중 바이트 정수는 별도 변경 합의가 없으면 little-endian으로 전송한다.

## 5. 안전 규칙

- 모터1 정지 확인 전 캐리어 정렬을 시작하지 않는다.
- 모터2 정지 확인 전 클러치 모터를 동작하지 않는다.
- 모터3 동작 중 모터1과 모터2는 정지 상태여야 한다.
- 클러치 b 체결 확인 전 트랙 주행을 허용하지 않는다.
- 클러치 a 체결 확인 전 캐리어 원위치 이동과 휠 주행을 허용하지 않는다.
- 센서 이상, 통신 두절, 제한 시간 초과 또는 유효하지 않은 상태 전이가 발생하면 모든 모터를 정지한다.
- 실제 기구 시험은 저속·저출력, 무부하, 단일 모듈, 전체 모듈 순으로 확대한다.
- 안전 로직을 제거하거나 우회하는 변경은 이유와 시험 계획 없이 병합하지 않는다.

## 6. 문서화 규칙

모든 중요 설계 결정, 하드웨어 변경, 통신 규격 변경, 실기 시험 결과와 troubleshooting 내용을 `agents/docs/`에 기록한다.

코드를 수정하는 모든 작업은 작업 단위마다 `agents/docs/코드_변경_기록/`에 새 변경 기록을 작성한다. 기존 기록을 덮어써서 새 작업을 기록하지 않는다.

- 파일 이름은 변경 작업을 시작한 한국 표준시 기준 `YYYYMMDD_HHMMSS_간단한_변경명.md` 형식을 사용한다.
- 문서의 첫 제목도 `# YYYYMMDD_HHMMSS 변경 제목`처럼 같은 타임스탬프로 시작한다.
- 하나의 논리적 변경 작업에는 기록 파일 하나를 사용하고, 그 작업 중 추가 수정과 검증 결과는 같은 파일에 이어서 갱신한다.
- 서로 목적이 다른 후속 변경을 시작하면 새로운 타임스탬프의 문서를 만든다.
- 소스 코드뿐 아니라 동작에 영향을 주는 프로젝트 설정, 통신 규격, 테스트 코드 변경도 기록 대상이다.
- 기록에는 변경 목적, 변경 전 동작, 수정 파일과 심볼, 상세 변경 내용과 이유, 영향 범위, 프로토콜 호환성, 안전 영향, 검증 결과, 미검증 항목, 남은 위험과 후속 작업을 포함한다.
- 작성 방법과 양식은 `agents/docs/코드_변경_기록/README.md`와 `TEMPLATE.md`를 따른다.

새 troubleshooting 문서 이름은 한국 표준시 기준 다음 형식을 사용한다.

```text
YYYYMMDD_HHMMSS_간단한_문제명.md
```

예:

```text
20260902_153045_I2C_응답_시간초과.md
```

문서에는 최소한 다음 항목을 포함한다.

```markdown
# 문제 제목

## 발생 시각과 환경
## 사용한 코드 버전 또는 Git 커밋
## 증상
## 재현 절차
## 기대 동작
## 조사 내용과 측정값
## 원인
## 해결 또는 임시 조치
## 검증 결과
## 남은 위험과 후속 작업
```

- 원인을 확정하지 못했으면 추측을 사실처럼 쓰지 않고 `미확정`으로 표시한다.
- 오실로스코프, 로직 분석기, 전압, 전류, 엔코더 값 등 근거를 기록한다.
- 해결되지 않은 문제도 기록하고 임시 조치와 다음 확인 항목을 남긴다.
- 같은 문제의 후속 내용은 기존 문서에 시간순으로 추가하고, 성격이 다른 문제는 새 문서를 만든다.
- 이미지와 로그는 관련 문서와 같은 이름의 하위 폴더 또는 명확한 파일명으로 보관한다.

## 7. 코드 변경 규칙

- 생성된 빌드 파일보다 원본 소스와 프로젝트 설정 파일을 수정한다.
- STM32CubeMX가 재생성할 수 있는 파일의 사용자 코드는 `USER CODE BEGIN/END` 구역에 둔다.
- 핀, 타이머, I2C 주소, 감속비와 센서 분해능을 설명 없이 매직 넘버로 추가하지 않는다.
- 물리 상수는 단위가 드러나는 이름을 사용한다. 예: `carrier_angle_tenths`, `clutch_timeout_ms`.
- 모터 명령에는 범위 제한, 제한 시간과 실패 시 정지 동작을 둔다.
- 기존 사용자 변경과 무관한 파일을 함께 포맷하거나 수정하지 않는다.

## 8. 검증 및 보고

- Python 변경은 문법 검사와 가능한 단위 테스트를 수행한다.
- ESP32 변경은 컴파일 결과와 대상 보드 설정을 기록한다.
- STM32 변경은 컴파일 결과, 플래시 대상과 실제 시험 여부를 구분해 기록한다.
- 하드웨어에서 검증하지 않은 기능은 `컴파일 확인`과 `실기 검증 완료`를 구분한다.
- 작업 완료 보고에는 수정 파일, 수행한 검증, 확인하지 못한 항목과 다음 작업을 포함한다.

## 9. Git 협업 규칙

- `main`에서 직접 기능을 개발하지 않고 `feature/`, `fix/`, `docs/`, `test/` 브랜치를 사용한다.
- 커밋 하나는 하나의 목적만 담는다.
- 공용 프로토콜 변경은 관련 담당자 모두의 검토 후 병합한다.
- Pull Request에 변경 목적, 영향 범위, 시험 결과와 남은 문제를 기록한다.
- 비밀키, 개인 포트 설정, 빌드 결과물, IDE 워크스페이스 메타데이터와 임시 백업을 커밋하지 않는다.
